"""
ContextIQ MCP server entrypoint.

Placeholder for Phase 1. Implemented in Phase 6 (MCP Server) — will expose
context-layer capabilities (search_assets, get_lineage, etc.) as MCP tools
by calling the SAME service layer used by the FastAPI backend
(backend/app/services), not a duplicate implementation.
"""
